# a = None
# if(a is None):  # this will be true when a is also pointing to None to which None is pointing. So,( a is b ) means a and b are pointing to same location.
#     print("yes") # is differs with == because ( a == b ) means it will check the values of a and b separately and then compare those two.
# else:
#     print("No")

# a = [45, 56, 6]
# print (453 in a)

b = 5466

print (54 in b)